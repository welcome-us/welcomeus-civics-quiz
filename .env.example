# Anthropic API key used by the server-side answer grader (app/api/grade).
# Copy this file to `.env.local` and fill in a real key.
# Without it, the app falls back to the deterministic browser matcher.
ANTHROPIC_API_KEY=
